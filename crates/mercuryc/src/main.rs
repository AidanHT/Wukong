//! `mercuryc` — the Mercury compiler driver.
//!
//! This is currently a skeleton; real argument parsing and pipeline wiring arrive as the
//! compiler stages come online.

fn main() {
    println!("mercuryc {} — the Mercury compiler", env!("CARGO_PKG_VERSION"));
    println!("(no input handling yet — the pipeline is under construction)");
}
