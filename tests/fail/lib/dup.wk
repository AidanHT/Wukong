// Imported by tests/fail/import_duplicate.wk: defines the same `clash` as the root file.
// (The fail harness scans tests/fail/*.wk non-recursively, so this library is never compiled
// standalone.)
module lib.dup

fn clash() -> i32 {
    return 2;
}
